<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Workspace files (agent's downloads) -->
    <files-path name="workspace" path="workspace/" />
    <!-- Bootstrap root (for diagnostic exports) -->
    <files-path name="files" path="." />
</paths>
